import type { ReactNode } from "react";

export const metadata = {
  title: "Contact Ambel POS | Get Your Store Ready to Bill",
  description: "Reach Ambel POS by email, phone or WhatsApp. Send your catalogue, supplier bills or product list and we'll get your billing and stock connected.",
};

export default function ContactLayout({ children }: { children: ReactNode }) {
  return children;
}
